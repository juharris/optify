name: "[Elixir] Build & Test"

on:
  push:
    branches: [ "main" ]
    paths:
      - ".github/workflows/elixir_build_test.yml"
      - "elixir/**"
      - "rust/optify/**"
      - "tests/**"
  pull_request:
    branches: [ "main" ]
    types: [ opened, synchronize, reopened, ready_for_review ]
    paths:
      - ".github/workflows/elixir_build_test.yml"
      - "elixir/**"
      - "rust/optify/**"
      - "tests/**"

  # Allows you to run this workflow manually from the Actions tab.
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  MIX_ENV: test

jobs:
  setup:
    # Drafts: Only run on Ubuntu.
    # Non-drafts: Run on both Ubuntu and macOS.
    name: "Setup Build Matrix"
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.setup.outputs.matrix }}
    steps:
      - id: setup
        run: |
          if [[ "${{ github.event.pull_request.draft }}" == "true" ]]; then
            matrix='["ubuntu-latest"]'
          else
            matrix='["ubuntu-latest", "macos-latest"]'
          fi
          echo "matrix=$matrix" >> $GITHUB_OUTPUT

  test:
    name: "Build & Test"
    needs: setup
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        os: ${{ fromJson(needs.setup.outputs.matrix) }}
    steps:
    - uses: actions/checkout@v6

    - name: "[Elixir] Setup Erlang & Elixir"
      uses: erlef/setup-beam@v1
      with:
        otp-version: "28"
        elixir-version: "1.18"

    - name: "[Rust] Setup cache"
      uses: Swatinem/rust-cache@v2
      with:
        workspaces: |
          elixir/optify
          rust/optify
        cache-on-failure: true
        shared-key: ${{ matrix.os }}-rust

    - name: "[Elixir] Restore deps cache"
      uses: actions/cache@v5
      with:
        path: |
          elixir/optify/_build
          elixir/optify/deps
          elixir/optify/priv/native
        key: ${{ matrix.os }}-mix-${{ hashFiles('elixir/optify/mix.lock', 'elixir/optify/Cargo.toml', 'elixir/optify/Cargo.lock', 'elixir/optify/native/optify_nif/Cargo.toml', 'elixir/optify/native/optify_nif/src/**', 'rust/optify/src/**', 'rust/optify/Cargo.toml') }}
        restore-keys: |
          ${{ matrix.os }}-mix-

    - name: "[Elixir] Get dependencies"
      run: mix deps.get
      working-directory: ./elixir/optify

    - name: "[Elixir] Compile"
      run: mix compile --warnings-as-errors
      working-directory: ./elixir/optify

    - name: "[Elixir] Check formatting"
      run: mix format --check-formatted
      working-directory: ./elixir/optify

    - name: "[Rust] Check formatting"
      run: cargo fmt -- --check
      working-directory: ./elixir/optify/native/optify_nif

    - name: "[Rust] Clippy"
      run: cargo clippy --no-deps -- -D warnings
      working-directory: ./elixir/optify/native/optify_nif

    - name: "[Elixir] Run tests"
      run: mix test
      working-directory: ./elixir/optify

    - name: "[Elixir] Build docs"
      if: matrix.os == 'ubuntu-latest'
      env:
        MIX_ENV: dev
      run: mix docs
      working-directory: ./elixir/optify

    - name: "[Elixir] Publish"
      if: github.ref == 'refs/heads/main' && matrix.os == 'ubuntu-latest'
      shell: bash
      continue-on-error: true
      env:
        HEX_API_KEY: ${{ secrets.HEX_API_KEY }}
        MIX_ENV: dev
      run: |
        mix hex.publish --yes
      working-directory: ./elixir/optify

    - name: "[Elixir] Publish Checks"
      if: github.ref != 'refs/heads/main' && matrix.os == 'ubuntu-latest'
      shell: bash
      env:
        HEX_API_KEY: ${{ secrets.HEX_API_KEY }}
        MIX_ENV: dev
      run: |
        mix hex.publish --dry-run --yes
      working-directory: ./elixir/optify
